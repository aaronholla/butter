# Migration `20190901130029-init`

This migration has been generated by Aaron Holland at 9/1/2019, 1:00:29 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."Movie"("id" text NOT NULL  ,"title" text NOT NULL DEFAULT '' ,PRIMARY KEY ("id"))
;

CREATE TABLE "public"."User"("email" text NOT NULL DEFAULT '' ,"id" text NOT NULL  ,"name" text   ,PRIMARY KEY ("id"))
;

CREATE TABLE "public"."Vote"("id" text NOT NULL  ,"scene" boolean NOT NULL DEFAULT false ,PRIMARY KEY ("id"))
;

ALTER TABLE "public"."Vote" ADD COLUMN "movie" text   REFERENCES "public"."Movie"("id") ON DELETE SET NULL,ADD COLUMN "user" text   REFERENCES "public"."User"("id") ON DELETE SET NULL;

CREATE UNIQUE INDEX "Movie.id" ON "public"."Movie"("id")

CREATE UNIQUE INDEX "User.id" ON "public"."User"("id")

CREATE UNIQUE INDEX "User.email" ON "public"."User"("email")

CREATE UNIQUE INDEX "Vote.id" ON "public"."Vote"("id")
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..20190901130029-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,28 @@
+generator photon {
+  provider = "photonjs"
+}
+
+datasource db {
+  provider = "postgresql"
+  url      = "postgresql://aaron@localhost:5432/butter_development?schema=public"
+}
+
+model Movie {
+  id    String @default(cuid()) @id @unique
+  title String
+  vote  Vote[]
+}
+
+model User {
+  id    String  @default(cuid()) @id @unique
+  email String  @unique
+  name  String?
+  vote  Vote[]
+}
+
+model Vote {
+  id    String  @default(cuid()) @id @unique
+  movie Movie?
+  scene Boolean
+  user  User?
+}
```

## Photon Usage

You can use a specific Photon built for this migration (20190901130029-init)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190901130029-init'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
